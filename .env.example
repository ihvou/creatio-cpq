# Supabase — powers the cross-device QR temp link (Scenario 2) and quote retrieval.
# Leave BOTH empty to run the demo single-device (the app degrades gracefully).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
